[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "caldav>=2.0.1",
    "flask>=3.1.1",
    "requests>=2.32.4",
    "waitress>=3.0.2",
]
